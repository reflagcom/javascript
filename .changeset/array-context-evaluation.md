---
"@reflag/flag-evaluation": minor
---

Add type-preserving array context evaluation with `ANY_OF`, `NOT_ANY_OF`, `SET`, and `NOT_SET` semantics, including compatibility with legacy JSON-encoded arrays.
